import 'package:taller/app/controllers/home_controller.dart';
import 'package:taller/app/ui/pages/login_page.dart';
import 'package:firebase_auth/firebase_auth.dart';
import 'package:flutter/material.dart';
import 'package:get/get.dart';

import '../../controllers/auth_controller.dart';

class HomePage extends StatelessWidget {
  HomePage({super.key});
  final user = FirebaseAuth.instance.currentUser!;
  @override
  Widget build(BuildContext context) {
    // change read to watch!!!!
    final AuthController authCntrl = Get.find<AuthController>();
    final HomeController homeCntrl = Get.put(HomeController());
    return Scaffold(
      body: SafeArea(
        child: SingleChildScrollView(
          child: Center(
            child: Column(
              mainAxisAlignment: MainAxisAlignment.center,
              crossAxisAlignment: CrossAxisAlignment.center,
              children: <Widget>[
                authCntrl.imageUrl != null &&
                        authCntrl.imageUrl!.value.isNotEmpty
                    ? CircleAvatar(
                        backgroundColor: Colors.white,
                        backgroundImage: NetworkImage("${authCntrl.imageUrl}"),
                        radius: 50,
                      )
                    : Container(
                        width: 100,
                        height: 100,
                        decoration: const BoxDecoration(
                          color: Colors.grey,
                          shape: BoxShape.circle,
                        ),
                        child: const Center(
                          child: Text(
                            "No hay imagen disponible",
                            textAlign: TextAlign.center,
                            style: TextStyle(
                              color: Colors.white,
                            ),
                          ),
                        ),
                      ),
                const SizedBox(
                  height: 20,
                ),
                Text(
                  "Welcome ${authCntrl.name}",
                  style: const TextStyle(
                      fontSize: 15, fontWeight: FontWeight.w500),
                ),
                const SizedBox(
                  height: 10,
                ),
                Text(
                  "${authCntrl.email}",
                  style: const TextStyle(
                      fontSize: 12, fontWeight: FontWeight.w500),
                ),
                const SizedBox(
                  height: 10,
                ),
                Text(
                  "${authCntrl.uid}",
                  style: const TextStyle(
                      fontSize: 12, fontWeight: FontWeight.w500),
                ),
                Text(
                  "${authCntrl.token}",
                  style: const TextStyle(
                      fontSize: 12, fontWeight: FontWeight.w500),
                ),
                const SizedBox(
                  height: 10,
                ),
                Row(
                  crossAxisAlignment: CrossAxisAlignment.center,
                  mainAxisAlignment: MainAxisAlignment.center,
                  children: [
                    const Text("PROVIDER:"),
                    const SizedBox(
                      width: 5,
                    ),
                    Text('${authCntrl.provider}',
                        style: const TextStyle(color: Colors.red)),
                  ],
                ),
                const SizedBox(
                  height: 20,
                ),
                ElevatedButton(
                    onPressed: () {
                      authCntrl.userSignOut();
                      Get.offAll(() => LoginPage());
                    },
                    child: const Text("SIGNOUT",
                        style: TextStyle(
                          color: Colors.white,
                        )))
              ],
            ),
          ),
        ),
      ),
    );
  }
}
